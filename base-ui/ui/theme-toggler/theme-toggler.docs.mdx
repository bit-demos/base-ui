---
description: 'A ThemeToggler component.'
labels: ['theme', 'toggler']
---

import { ThemeToggler } from './theme-toggler';

When button is clicked it adds a class to the body of light or dark so we can then set colors based which is selected.

### Component usage

```js
<ThemeToggler />
```
